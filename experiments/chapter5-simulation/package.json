{
  "name": "@ots/chapter5-simulation",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/artifact/*.test.ts src/behavior/*.test.ts src/core/*.test.ts src/disclosure/*.test.ts src/liquidity/*.test.ts src/metrics/*.test.ts src/network/*.test.ts src/observation/*.test.ts src/oracle/*.test.ts src/redemption/*.test.ts src/risk/*.test.ts src/shocks/*.test.ts src/state/*.test.ts",
    "lock:write": "node scripts/foundation-lock.mjs --write",
    "lock:check": "node scripts/foundation-lock.mjs --check",
    "check": "pnpm typecheck && pnpm test && pnpm lock:check"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
