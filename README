# OTC Fund 生命周期可观测与透明度制度原型

本仓库实现论文第 3 章“区块链赋能的 OTC fund 生命周期可观测系统与透明度制度”的研究原型。系统目标不是复刻完整基金会计系统，而是验证以下闭环：

`生命周期事件 -> 风险指标 -> 链上评分与控制 -> R0-R4 权限披露 -> 四时间戳审计 -> 仿真导出`

前端不属于当前论文验收范围。仓库只保留 Solidity 合约、TypeScript API、审计索引、实验脚本和测试证据，避免旧页面对已更新的状态机和权限接口产生误导。

## 系统组成

- `contracts/src/FundToken.sol`：投资者准入、现金认购到份额换算、份额登记、赎回队列、按 NAV 计算现金结算和 Gate 拦截。
- `contracts/src/NAVRegistry.sol`：首期发行 NAV，以及按 `NetAssetValue / TotalShares` 计算的后续 NAV、NAVAdjustment 和 ValuationHaircut。
- `contracts/src/RiskRegistry.sol`：流动性状态、六项风险指标、版本化权重、链上评分、阈值控制、Swing Pricing 和 Side Pocket 状态。
- `apps/api/src/routes/`：角色级访问控制、链上读写、公开/监管披露端点和审计导出。
- `apps/api/src/risk/regimes.ts`：以五维参数包实现 R0-R4。
- `apps/api/src/audit/`：事件索引、四时间戳、承诺哈希和 CSV/JSON 导出。
- `apps/api/src/risk/subscription-flow.ts`、`settlement-delay.ts`：从生命周期事件派生认购流入与赎回结算时延，仅供审计/仿真。
- `apps/api/src/simulation/`：三类 DetectionLag，以及陈旧期限、权重和 κ 的联合敏感性分析。
- `scenarios/`：DetectionLag 情景的参数口径定义（tau、轮询间隔等）。冲击真值 `shockAt` 只存在于链下情景参数中，不向业务事件流注入伪造的“冲击事件”；端到端 smoke 以运行时时钟实例化同一组参数（文件中的 `shockAt` 为占位示例值）。

更完整的论文映射见 `docs/chapter3-system-implementation.md`，测试与评价维度映射见 `docs/chapter3-test-matrix.md`。

## 技术栈

- Solidity `^0.8.30`（Foundry 工具链锁定 solc 0.8.30）、Foundry、Anvil
- OpenZeppelin Contracts 5.4.0
- Node.js 22、TypeScript strict mode、Fastify、Viem、Zod
- PostgreSQL 16（可选持久化；CI 强制验证）
- pnpm 9.6 workspace
- GitHub Actions

## 基线参数

所有参数都是研究原型的可配置基线，不是论文或监管规则规定值：

- 风险指标与权重使用 basis points，`10000 = 100%`。
- 默认六项权重为近似等权：`[1667,1667,1667,1667,1666,1666]`。
- `MaxStaleAge = 30 days`，并测试 7/14/30/45 天。
- 展示阈值：yellow `4000`、red `6000`。
- 默认 Gate 阈值：`kappa = 7000`，仅当 `score > kappa` 自动触发。
- 稳健性检验扫描 `kappa in {5000, 6000, 7000, 8000}`。
- DetectionLag 情景阈值 `tau = 6000`，与干预阈值分离。

## 安装

前置环境：

- Node.js 22+
- pnpm 9.6
- Foundry（`forge`、`anvil`）

从仓库根目录安装 JavaScript 依赖：

```bash
pnpm install --frozen-lockfile
```

安装锁定的 Foundry 依赖：

```bash
cd contracts
forge install foundry-rs/forge-std@v1.10.0 --no-git
forge install OpenZeppelin/openzeppelin-contracts@v5.4.0 --no-git
```

## 本地运行

1. 启动 Anvil：

```bash
pnpm dev:anvil
```

2. 在另一个终端部署合约：

```bash
cd contracts
forge script script/Deploy.s.sol:Deploy \
  --rpc-url http://127.0.0.1:8545 \
  --broadcast \
  --slow
```

部署脚本从环境变量读取管理人、风险 Oracle、监管者、流动性 Oracle 私钥、`FUND_ID_LABEL`、`KAPPA_BPS`、`MAX_STALE_AGE_SEC` 和六项 `RISK_WEIGHT_*_BPS`。未提供研究参数时使用上述基线；合约仍强制权重总和为 10000、κ 位于 0..10000 且陈旧期限为正数。部署脚本和 API 从同一 `FUND_ID_LABEL` 派生 `fundId`，并通过部署脚本测试与端到端 Gate 测试降低配置错配风险；原型未实现 fundId 与合约地址之间的中央链上登记约束（见 `docs/chapter3-system-implementation.md` 第 10 节）。

3. 配置并启动 API：

```bash
cp apps/api/.env.example apps/api/.env
pnpm dev:api
```

把部署输出的三个合约地址写入 `apps/api/.env`，并用 `API_KEY_INVESTOR_ADDRESS` 将投资者 API Key 绑定到演示持有人地址。API 启动时会读取 `FundToken` 的 immutable `fundId` 与 NAVRegistry 地址并校验本地配置，错配时拒绝启动。该文件已被 `.gitignore` 排除，不得提交私钥或 API Key。

`MAX_VALUATION_HAIRCUT_AGE_SEC` 和 `MAX_LIQUIDITY_BUFFER_AGE_SEC` 为可选来源年龄警告阈值；缺省时采用受信 Oracle 最新状态，不擅自设定硬 SLA。配置阈值后，风险响应记录 `fresh`/`stale_warning`，但不会修改评分或拒绝提交。

4. 健康检查：

```bash
curl http://127.0.0.1:3001/health
```

## 完整测试

从仓库根目录运行：

```bash
pnpm test:chapter3
```

该命令依次执行：

1. Foundry 合约单元测试与 Fuzz。
2. Foundry 有界 Handler 状态不变量测试。
3. gas snapshot 检查与生产合约行覆盖率。
4. TypeScript 静态检查。
5. API、制度引擎、审计和仿真单元测试。
6. 独立 Anvil 部署。
7. 生命周期、评分、控制、R0-R4、审计和 receipt gas 端到端 smoke。
8. 提供 `CHAPTER3_DATABASE_URL` 时，重启 API 验证 PostgreSQL 持久化。

审计者可通过 `/audit/subscription-flow`、`/audit/settlement-delays` 查询派生指标；`/audit/simulation` 同时导出两者。它们不进入 `/risk/submit` 的 payload v3 或六项链上评分。

机器可读证据写入：

```text
test-results/chapter3/<UTC timestamp>/summary.json
```

GitHub Actions 使用 PostgreSQL 16 执行同一入口，并始终上传测试证据。

## 原型边界

- Swing Pricing 和 Side Pocket 实现事件、状态标志、触发依据与承诺哈希，不实现完整资产会计。
- Gate 采用 fail-closed 的全额冻结语义：未配置 RiskRegistry 时拒绝赎回；配置后拦截新增赎回和存量队列结算；触发自动，解除由监管角色提交原因哈希。
- API Key 是研究原型级认证，不等同生产身份系统；投资者 Key 绑定单一地址并强制“只查本人”，但不实现用户生命周期、密钥轮换或多地址身份管理。
- 许可链原始日志仍可能包含地址；VC 哈希和 payload commitment 不等同零知识证明。
- R0 每 604800 秒周期披露一次，边界按 Unix epoch 对齐，单条记录等待时间在 0–7 天之间；生产制度可替换为自然周或业务日历。
- 风险提交在一个固定区块读取权重、NAV、流动性、份额和赎回状态；实时端点默认只接受距该区块不超过 300 秒的 `occurredAt`。
- 链上 `redemptionPressureBps` 按窗口期申请量计算，是前瞻性 RedemptionRequestPressure；申请量与结算量分别导出。
- `SubscriptionFlow` 同时保留申请金额、接受金额与 mint 份额；`SettlementDelay` 只对已结算请求计算，未结算请求标记为 `pending`。
- 基金成立前使用一次性的初始发行 NAV；后续 NAV、认购份额和赎回现金金额由合约按定点数公式计算。原型不转移现金资产，也不实现完整基金总账。
- 当前为单基金部署实例，通过 immutable 绑定与 API 启动校验避免 `fundId`/NAVRegistry 错配；不声称实现全局多基金 FundRegistry。
- API 从同一区块派生六项指标，但链上 `RISK_ORACLE_ROLE` 仍是受信角色；其绕过 API 直接调用合约时，链上保证评分复算和控制规则，不证明指标数据来源。
- 审计索引依赖许可链稳定最终性假设；原型不实现确认区块深度和链重组回滚，生产部署需在确认后固化索引并支持孤块撤销与重放。
- 无 PostgreSQL 时生命周期索引保留完整内存事件且不做淘汰，只适用于短期演示；API 访问审计使用独立的 5000 条有界缓冲。
